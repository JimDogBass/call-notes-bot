aiohttp
docxtpl
python-docx
pdfplumber
pymupdf
openai
msal
requests
# Optional OCR fallback for scanned PDFs — needs poppler + tesseract installed
# in the Railway container. Comment out if not provisioning those.
pdf2image
pytesseract
